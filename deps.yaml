dependencies:
  # Zig compiler (temporary until Home compiler is ready)
  - ziglang.org@0.15.1

  # QEMU for testing
  - qemu.org

# NOTE: GRUB and xorriso not yet in pantry registry
# Install manually via brew:
# brew install grub xorriso

env:
  KERNEL_DIR: /Users/chrisbreuer/Code/home-os/kernel
  ISO_DIR: $KERNEL_DIR/iso
  BUILD_DIR: $KERNEL_DIR/build
