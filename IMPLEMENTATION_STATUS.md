# Implementation Status

> **Auto-generated by `scripts/generate_status.py`** — do not edit by hand.
> Every figure below is measured when the script runs, never restated, so
> this page cannot drift from the repository ([MASTER_PLAN §13.2](docs/MASTER_PLAN.md)).

## Where the project actually is

The Home-compiled kernel **boots and runs its full initialisation**: memory
management, the scheduler, the security subsystems, drivers, filesystems,
networking and system services all initialise on the serial console. The
milestones are listed in `scripts/boot-milestones.txt` and checked on every
build.

What executes is the Appendix A set. The rest of this repository is source
that has been written and parsed, but never run.

## Parse rate

**419/419 kernel `.home` files parse (100%)**

- Compiler: `home-lang/home` @ `bb52bd4ab`
- Every kernel file parses. This is milestone A1.

Parsing is not compiling. A file in this count has been accepted by the
parser; it has not been typechecked, code-generated, linked, or run.

## Boot status

✅ **`boot-qemu-x86_64`: PASS** — serial says `HomeOS v0.1: kernel_main reached`

Measured by building `kernel/src/mvk_poc.home` through the Home compiler,
linking it with `kernel/src/boot.s` via `kernel/linker.ld`, and booting the
result in QEMU with the serial console captured.

✅ **`boot-full-kernel`: PASS** — 210/210 init milestones reached, through to the end of init

The line above measures the proof-of-life kernel: one file that prints and
halts. This one measures the real kernel — every Appendix A file linked into
one image — against the milestone list in `scripts/boot-milestones.txt`,
which names one subsystem per entry and may only ever grow.

✅ **`boot-qemu-aarch64`: PASS** — serial says `HomeOS v0.1: aarch64 kernel_main reached`

Measured by building `kernel/src/arm64_poc.home` for
`aarch64-freestanding`, linking it with `kernel/src/arch/arm64/boot.s` via
`kernel/linker-virt.ld`, and booting it on QEMU's `virt` machine.

**This is not a Raspberry Pi.** QEMU has no Pi 5 machine model — there is no
RP1 — so a pass here means the compiler, the frame layout, the MMIO path and
the boot handoff work. It says nothing about the Pi's own peripherals, which
only the hardware gate can measure.

## Codegen ratchet

**85/85 of the Minimum Viable Kernel file set reaches codegen.**

This is the number to watch. The MVK set is
[MASTER_PLAN Appendix A](docs/MASTER_PLAN.md#appendix-a--minimum-viable-kernel-file-set);
a file counts only when the compiler produces assembly with no unlowered
construct in it *and* the assembler accepts that assembly. It may never
fall — `scripts/mvk-compiles.sh` fails the build if it does.

Run `scripts/mvk-compiles.sh --list` to see what each remaining file is
waiting on; the failures name the construct, not just the count.

**85/85 of the same set reaches codegen for `aarch64`.**

Kept as its own number rather than averaged in, because the two targets
advance independently. The gap is not a compiler gap: the files that do
not lower for ARM are the ones reached by `asm volatile` blocks written
in x86 assembly, which is emitted verbatim by definition. Making those
architecture-neutral is kernel work, not backend work.

## Source inventory

| Area | `.home` files | Lines |
|------|--------------:|------:|
| `kernel/` | 419 | 253,332 |
| `apps/` | 125 | 16,048 |
| `libs/` | 12 | 7,240 |
| `installer/` | 1 | 1,026 |

Breadth is not progress. These files are the corpus the compiler and the
bring-up work are measured against, not a list of working features.

## Stub-Burndown Register

Parsed from [MASTER_PLAN §7](docs/MASTER_PLAN.md#7-workstream-b--kernel--stub-burndown-register),
cross-checked against `// STUB(Sn)` markers in source by the `stub-register`
CI gate (`scripts/stub-check.sh`).

| # | Stub | File | Markers | Status |
|---|------|------|--------:|--------|
| S1 | silent hlt-stub fallback + dead `.zig` references | `scripts/build.sh` | 0 | **CLOSED** |
| S3 | the RX path parses Ethernet, demuxes on ethertype, validates IPv4 and dispatches to arp/icmp/udp | `kernel/src/net/netdev.home` | 0 | **CLOSED** |
| S4 | steps 1–6 of `docs/design/homefs.md` §9: on-disk structures with blake2s checksums, a block device, copy-on-write B-trees, the commit protocol, snapshots with rollback, objects (files, directories and extent trees), removal, ownership and mode, `fsck`, free-space reclamation, and VFS routing — a volume mounts at a path and `open`/`read`/`write`/`lseek`/`readdir`/`mkdir`/`rmdir` reach it, with permissions checked against the object's own mode and owner. A volume survives a reboot, `scripts/crash-gate.sh` kills QEMU mid-commit and remounts to prove it, and the boot gate runs 18 checks over the object layer plus an end-to-end round trip through /homefs | `kernel/src/fs/homefs.home` | 0 | **CLOSED** |
| S5 | chacha20, poly1305, blake2s and curve25519 are implemented and checked against the vectors in RFC 8439, RFC 7693 and RFC 7748 | `kernel/src/crypto/` | 0 | **CLOSED** |
| S6 | the xHCI host controller, USB mass storage and USB HID. Enumeration walks every port and dispatches on interface class; Bulk-Only Transport carries INQUIRY, READ CAPACITY(10), READ(10) and WRITE(10); the disk is registered as `/dev/usb0` so paths reach it through the VFS; and HID runs in boot protocol with both the mouse and keyboard report layouts decoded. The boot gate reads a known block, writes another and checks the host image (a device accepting a write is not the same as the write reaching the media), reads that block back through `vfs_pread`, moves an emulated mouse and reads the movement off the interrupt endpoint, and — in a second run, because a usb-kbd takes keystrokes away from the PS/2 controller — presses a key and reads the keycode back | `kernel/src/drivers/usb.home` | 0 | **CLOSED** |
| S7 | ACPI table discovery, the AML namespace and an interpreter, and S3 suspend/resume. The DSDT is parsed to the byte and shutdown takes its SLP_TYP from `\_S5_` instead of assuming 5; methods run within a stated integer subset and refuse anything outside it rather than guessing (15 of the 100 QEMU declares fall inside); and the machine suspends to RAM and comes back through a real-mode trampoline planted in the FACS waking vector. The boot gate requires the parser to reject a truncated and a corrupted DSDT, runs the interpreter against a hand-assembled method with exact expected values, and — in a run of its own, since suspending ends a run — suspends the machine, confirms from outside the guest that QEMU really slept, wakes it, and requires the kernel to report coming back | `kernel/src/drivers/acpi.home` | 0 | **CLOSED** |
| S8 | `mmio_read32`/`mmio_write32` were inert, so every ARM64/Pi driver was too | `kernel/src/arch/arm64/arm64.home` | 0 | **CLOSED** |
| S9 | the trap says why. Port-I/O wrappers still halt the machine on ARM64, and that is not the part that was wrong: the architecture has no I/O address space, so calling one is always a bug and stopping is the answer. What was wrong is that it stopped in silence, and it had to — every serial write in `foundation.home` reached the port through `outb`, so the one way it had to say why went through the very wrapper that trapped, and any ARM64 code that logged through foundation halted instead of logging. `serial_write_char` now writes to the PL011 on ARM64, at an address board init sets and which defaults to QEMU virt's, so `port_io_unavailable` prints its reason before halting; a re-entry guard stops the day something on the way to the UART reaches for a port again from becoming a stack overflow instead of a line. `kernel/src/arm64_poc.home` writes one line through foundation and then asks for a port on purpose, and the ARM64 gate requires both the announcement and the trap's own line — which are the last two the kernel prints, because the second call does not return | `kernel/src/core/foundation.home` | 0 | **CLOSED** |
| S10 | SMAP's ARM64 counterpart (the PAN bit in PSTATE) is set up, so `asm_stac`/`asm_clac` move a real bit on both architectures | `kernel/src/core/foundation.home` | 0 | **CLOSED** |
| S11 | `tcp_input()` validates a segment and hands it to the state machine, and netdev dispatches to it | `kernel/src/net/netdev.home` | 0 | **CLOSED** |
| S12 | `cmd &` starts a task and returns to the prompt, `jobs` lists what is running or done with its exit status, and `fg` gives a job the CPU until it finishes. The console gives jobs a turn between lines, so a job makes progress without being asked for. Scheduling is cooperative (S13), so a job runs at the turns it is given rather than truly alongside the shell, and a builtin cannot be backgrounded — it runs inside this shell and there is nothing to run it from, which is refused. `bg` is absent for the same reason a stopped job is: nothing stops a job, because there is no SIGTSTP from a terminal driver that does not exist yet. Pipelines run in stages rather than concurrently — each stage's output goes to a holding file and the next stage reads it, so for finite input the bytes and the exit status are what a concurrent pipe gives, but an unbounded producer never reaches its consumer. The boot gate runs a background job to completion and reads its status back out of `jobs`; `scripts/den-conform.sh` checks the whole shell against den | `kernel/src/console/den.home` | 0 | **CLOSED** |
| S13 | the timer takes the CPU from a task that has not offered it. fork returns twice into an address space of its own, children are runnable from the moment they are forked, and a task that makes no system call at all is now interrupted rather than holding the machine until it chooses to stop. Only ring 3 is preempted, and that restriction is the safety argument: this kernel has no locks, so interrupting a task inside a syscall and letting another call the same code would leave every structure shared with nothing to serialise them — an interrupted CS of 0x1b means the task holds nothing of the kernel's, which makes the switch exactly as safe as the cooperative one it borrows. What remains, and is stated at the site rather than hidden: a *kernel* loop still cannot be preempted, which is why the pipe waits and `do_wait` check for nothing else runnable instead of assuming someone will interrupt them. `userland/bin/spintest.home` makes no syscall between its first line and its last, and the gate asserts the scheduler's own count of preemptions — outcome cannot tell the two schedulers apart, since any child that terminates finishes either way, so what is asserted is that the timer really did take the CPU. Two tests had to stop asserting an interleaving that is no longer promised | `kernel/src/core/process.home` | 0 | **CLOSED** |
| S14 | none of the twenty reports having done something it did not do. `stat`/`fstat`/`lstat` fill the caller's buffer from the inode or the homefs object, and lstat reports the link where stat reports what it points at; `link` adds a second name and the inode's own count rises; `symlink` stores its target and `path_resolve` follows it, with a depth limit so a loop ends; `readlink` returns the target's length; `rename` is link-then-unlink, so the count survives the move; `access` checks the mode triad that applies; `chdir` records a working directory that `getcwd` reports back (it is recorded, not yet *used* — path_resolve walks from the root, which is stated at the site); `getdents` walks the directory's own blocks; `getppid` asks the process table instead of answering 1 for everyone; `nanosleep` spins on the timer for as long as it was asked to; `truncate`/`ftruncate` shorten the file. The rest — `mprotect`, `pipe`, `dup`, `poll`, `epoll_wait`, `send`, `recv`, `socket`, `ioctl` and this module's signal bookkeeping — return ENOSYS with the reason stated where they sit, because what they need does not exist yet: there is no socket table, no re-flagging of a mapped page, no reference count from the descriptor side, and signal delivery lives in `arch/x86_64/usermode.home`. Two path walkers were also merged into one, so `stat /a/./b` and `open /a/./b` can no longer disagree. `userland/bin/mdtest.home` asserts a value for each — a size, a link count, a parent pid — because a check that only asked whether the call failed would have passed against all twenty; the boot gate runs it and then repeats the same claims through `stat`, `ls`, `ln`, `mv` and `readlink` | `kernel/src/sys/syscall.home` | 0 | **CLOSED** |
| S15 | the function that could not switch is gone. A context switch cannot be an ordinary function, and that one was undone by its own epilogue; the working switch is `process_switch` in `kernel/src/idt_stubs.s`, driven by the cooperative scheduler in `arch/x86_64/usermode.home`. `scheduler_tick` no longer calls anything that claims to switch | `kernel/src/core/process.home` | 0 | **CLOSED** |
| S16 | the module no longer claims a delivery it cannot make. It could not enter a user handler: that means rewriting the frame the CPU will return through, and the frame belongs to whatever took the trap, not to a pid in this table — `arch/x86_64/usermode.home` does it, keyed by the task whose frame it holds. So `signal_set_handler` now accepts only SIG_DFL and SIG_IGN and refuses an address it would never jump to, and `signal_setup_frame` and `signal_return` — which printed that they were delivering and restoring, and did neither — are gone. What remains is real: queueing, masks, and the default actions, which terminate, stop, continue and ignore through `process.home`. `signal_deliver_pending` now *takes* those actions instead of returning a code for one; it answered 2 for "terminate" and both call sites in `main.home` discard the result, so a SIGKILL arriving there printed `fatal` and the process carried on | `kernel/src/sys/signal.home` | 0 | **CLOSED** |
| S17 | a key pressed on the machine's own keyboard now reaches the shell. `interrupt_dispatch` calls the driver's `keyboard_interrupt_handler` instead of reading port 0x60 and throwing the scancode away, so the driver decodes make and break codes, shift and caps, into its ring buffer; and `serial_shell_poll` drains that buffer into the same input path the serial line feeds, because a character is a character and the shell should not care which wire carried it. The gate no longer asserts on the interrupt count, which was the reason this survived a gate that presses keys: a run of its own types `uname -a` through the QEMU monitor and requires the shell's answer, which no echo of the keys typed can produce | `kernel/src/arch/x86_64/interrupts.home` | 0 | **CLOSED** |
| S18 | the filter is applied where the calls are. `syscall_entry_dispatch` in `arch/x86_64/usermode.home` consults the calling task's whitelist before `syscall_body` runs, so the two dozen calls that layer answers itself — `read`, `write`, `fork`, `wait`, `kill`, `getpid`, `nanosleep`, the signal and pipe paths — are covered for the first time; the kernel's table no longer filters at all, because it was keyed on `current_pid`, which nothing sets for a ring-3 task, and would have applied task 0's filter to whichever task fell through to it. `exit` and `sigreturn` are never refused: a task that cannot exit cannot be stopped and one that cannot return from a handler resumes nowhere. A task installs its own filter with `seccomp(list, count)` (110) and cannot lift or widen it; a child inherits it through `fork`, since a filter shed by forking is only a delay; and the loader clears the slot, so a filter does not outlive the program that asked for it. Two things the move made reachable were wrong and are fixed: `seccomp_init` never cleared the whitelist table, which is declared `undefined`, and strict mode's allowed set was Linux's numbers — 0, 1, 60, 15 — which in this kernel name exit, *fork*, clock_gettime and rmdir, so it permitted forking and refused writing. `userland/bin/sectest.home` asserts an allowed call still works, a denied one returns EPERM, and a forked child is still filtered | `kernel/src/arch/x86_64/usermode.home` | 0 | **CLOSED** |

0 of 17 entries open.

## Phase gates ([MASTER_PLAN §4](docs/MASTER_PLAN.md#4-the-phase-map))

One row per Tier-1 job name in [MASTER_PLAN §11](docs/MASTER_PLAN.md),
assigned to the phase §4 lists it under. A gate is green here because
something in this run produced the evidence for it — a line the boot gate
prints after checking from outside the guest, a milestone it asserted, or
a gate script of its own — never because the plan says it should be. A
gate with no probe has nothing enforcing it yet, and that is what "not
started" means below.

| Phase | Gate | Status |
|-------|------|--------|
| 0 | `parse-rate` | ✅ green |
| 0 | `stub-register` | ✅ pass — stub-register OK — 17 entries, none open |
| 0 | `boot-qemu-x86_64` | ✅ pass |
| 0.5 | `mvk-compiles` | ✅ green |
| 1 | `boot-to-shell` | ✅ green |
| 2 | `storage-roundtrip` | ✅ green — fsck OK: 8192 blocks, 3 file(s) [hello.txt, second.txt, written.txt], 8175 free blocks, clean |
| 2 | `net-echo` | ✅ green — net-echo both ways, host client got 32 bytes back |
| 2 | `fb-boot-log` | ✅ green — framebuffer 2359312 bytes, 3 distinct byte values |
| 3 | `libc-suite` | ✅ green — asserted as `libc-suite: every check passed` |
| 3 | `shell-suite` | ✅ green — 42 lines identical to the reference den, 2 script(s) |
| 3 | `coreutils-suite` | ✅ green — 56 distinct programs run from /bin under the boot gate |
| 3 | `pantry-local-install` | ✅ green — asserted as `[pantry] REFUSED: signature does not verify` |
| 4 | `craft-demo` | ⬜ not started — nothing enforces it yet |
| 4 | `wm-layouts` | ⬜ not started — nothing enforces it yet |
| 5 | `iso-install` | ⬜ not started — nothing enforces it yet |
| 5 | `desktop-parity-suite` | ⬜ not started — nothing enforces it yet |
| 6 | `snapshot-rollback` | ⬜ not started — nothing enforces it yet |
| 6 | `agent-cli-suite` | ⬜ not started — nothing enforces it yet |

