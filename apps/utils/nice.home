// home-os Nice
// Run command with modified priority

import "../../kernel/src/core/foundation.home" as foundation

export fn nice_main(priority: i32, command: u64) {
  foundation.vga_write_string("Running with priority ")
  foundation.vga_write_string(": ")
  foundation.vga_write_string(command)
  foundation.vga_write_string("\n")
  
  // Execute command with adjusted priority (stub)
}
