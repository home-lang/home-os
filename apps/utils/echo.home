// home-os Echo
// Display text

import "../../kernel/src/core/foundation.home" as foundation

export fn echo_main(text: u64, newline: u32) {
  foundation.vga_write_string(text)
  
  if newline == 1 {
    foundation.vga_write_string("\n")
  }
}
