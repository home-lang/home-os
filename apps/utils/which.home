// home-os Which
// Locate a command

import "../../kernel/src/core/foundation.home" as foundation
import "env.home" as env

export fn which_main(command: u64) {
  foundation.vga_write_string("Searching for: ")
  foundation.vga_write_string(command)
  foundation.vga_write_string("\n")
  
  var path: u64 = env.env_get("PATH")
  
  // Search in PATH directories (stub)
  foundation.vga_write_string("/usr/bin/")
  foundation.vga_write_string(command)
  foundation.vga_write_string("\n")
}
