// home-os Realpath
// Print resolved absolute path

import "../../kernel/src/core/foundation.home" as foundation

export fn realpath_main(path: u64) {
  // Resolve path (stub)
  foundation.vga_write_string("/home/user/")
  foundation.vga_write_string(path)
  foundation.vga_write_string("\n")
}
