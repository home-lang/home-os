// home-os Disk Usage
// Show directory space usage

import "../../kernel/src/core/foundation.home" as foundation
import "../../kernel/src/core/filesystem.home" as filesystem

fn du_scan_directory(path: u64, depth: u32) -> u64 {
  // Scan directory and calculate size (stub)
  return 1024 * 1024  // 1MB stub
}

export fn du_main(path: u64) {
  foundation.vga_clear()
  foundation.vga_write_string("=== Disk Usage ===\n\n")
  foundation.vga_write_string("Path: ")
  foundation.vga_write_string(path)
  foundation.vga_write_string("\n\n")
  
  var total: u64 = du_scan_directory(path, 0)
  
  foundation.vga_write_string("Total: ")
  foundation.vga_write_string(" bytes\n")
}
