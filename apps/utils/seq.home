// home-os Seq
// Print sequence of numbers

import "../../kernel/src/core/foundation.home" as foundation

export fn seq_main(start: u32, end: u32, step: u32) {
  if step == 0 { step = 1 }
  
  var i: u32 = start
  while i <= end {
    foundation.vga_write_string("\n")
    i = i + step
  }
}
