// true — exit 0, say nothing. POSIX's simplest utility, and this tree's
// first program written in Home rather than assembly.

import "../lib/libc.home" as libc

export fn _start(): never {
  libc.exit(0)
}
