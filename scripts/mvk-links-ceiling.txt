# The mvk-links ratchet ceiling: how many symbols were still undefined when
# the whole MASTER_PLAN Appendix A set was last linked together.
# scripts/mvk-links.sh fails if MORE are undefined than this — something new
# would then be referencing a function that was never written.
#
# This is a ceiling that must fall, not a floor that must rise. Duplicate
# symbols have no ceiling: they must stay at zero.
#
# History:
#   13 duplicates, 188 undefined  first link of the whole set. Every duplicate
#       was a real pair of distinct functions — serial.home and vga.home both
#       export writeChar — fixed by module-scoped symbols in
#       home-lang/home@b9d0582.
#  188  after that fix. These are references to functions that do not exist,
#       plus modules the Appendix A set imports but does not include.
188
