# The typecheck ratchet floor: how many MASTER_PLAN Appendix A files had zero
# type errors at the last measurement. scripts/typecheck.sh fails if fewer are
# clean. Same rules as scripts/mvk-floor.txt: raise it when the count advances;
# lower it only for a stated reason (a file left Appendix A, the check got
# stricter, or the compiler pin moved back deliberately).
#
# History:
#   35  first measurement — milestone A2 was almost done and unmeasured.
#   37  arbitrary-width integers (u4, u6, u40) added to the type system.
#   39  complete. if-expression branch parsing, bindings that survive a failed
#       inference, and pointer slicing.
#   38  integration.home removed from Appendix A (set size 39 -> 38); still
#       every file in the set.
#   35  net/socket, net/tcp and net/udp removed from Appendix A (set size
#       38 -> 35); still every file in the set.
#   48  same 14-file Appendix A growth; dns.home deferred (26 diagnostics).
#   49  den_lib.home, once the ownership tracker stopped leaking parameter
#       state across functions.
#   54  import-closure modules added to Appendix A, all already clean.
#   56  drivers/pci and drivers/e1000 added to Appendix A.
#   58  mm/swap and security/random added, after fixing a precedence bug and
#       a duplicate definition in them.
#   60  block/io_scheduler and block/request_merge added.
#   61  core/driver_init added.
#   64  net/socket, net/tcp and net/arp added — the set is import-closed.
#   80  console/den.home added (the shell itself, clean) and lib/den_lib.home
#       removed from the set (also clean, so the count falls by one as the set
#       does: 84 -> 83 files, 81 -> 80 clean). The jump from 64 is not new work
#       — those files had been clean and unmeasured since the 64 mark.
#   81  crypto/rsa.home added to the set when it gained a known-answer test.
#   82  pkg/pantry_local.home added — the signed-package installer.
#   85  crypto/chacha20, crypto/blake2s and crypto/curve25519 — the whole set.
#       Not work in this repo: the Home type checker gave `w[i]` the array type
#       when w is `*[u32; 16]`, so every use of the element failed as a
#       non-integer. Those three pass a fixed buffer by reference that way and
#       were the only files that did. Fixed in the compiler
#       (packages/types/src/type_system.zig); the backend had always lowered
#       them correctly, which is why their RFC known-answer tests have been
#       passing at boot the whole time.
90
