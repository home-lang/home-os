# The mvk-compiles ratchet floor: how many MASTER_PLAN Appendix A files
# reached codegen at the last measurement. scripts/mvk-compiles.sh fails if
# fewer compile than this.
#
# Raise it whenever the count advances. Lowering it is legitimate for exactly
# three reasons, each of which must be stated in the commit message: a file
# was removed from Appendix A, the check itself got stricter, or the compiler
# pin moved backwards deliberately. Anything else is a regression.
#
# History:
#   34  measured before the backend reported unsupported constructs. Inflated:
#       the codegen silently emitted nothing for expressions it did not
#       recognise, so files counted as compiling while dropping 1,235
#       constructs between them. See home-lang/home@6e014ab.
#   28  first honest measurement.
#   29  struct literals for bitfield types, function addresses, str indexing.
#   32  if-expression branches parse as blocks, so whole functions stopped
#       being silently truncated.
#   33  socket.home, once TCPConnectionInfo and tcp_accept existed.
#   35  slice lowering; frame slots inside expression bodies; if-expressions
#       carry a type; address-of is a pointer.
#   36  structs returned by value through a hidden destination pointer.
#   37  array literals and repeats, including arrays of structs; wide bitfield
#       containers; @sizeOf(@TypeOf(x)).
#   39  complete — every Appendix A file reaches codegen. Module-level array
#       tables, string-literal addresses, and located refusal markers.
#   38  integration.home removed from Appendix A — it initialises the package
#       manager, the den shell and the Craft GUI toolkit, none of which belong
#       in a minimum viable kernel. Set size 39 -> 38; still every file.
#   35  net/socket, net/tcp and net/udp removed from Appendix A — the
#       simplification the plan already recommended. Set size 38 -> 35; still
#       every file in the set.
#   49  Appendix A grew by 14 import-closure files (#41): boot_opt, aslr,
#       smep_smap, audit, vfs_permissions, ata, nvme, devfs, procfs, sysfs,
#       tmpfs, async_io, dhcp, den_lib. Every file in the set reaches codegen.
#   54  import-closure modules added to Appendix A (ptr_safety, capabilities,
#       audit, vfs_permissions, memcg, boot_opt, async_io, timer, ata, nvme,
#       udp, dns) — each already compiled clean.
#   56  drivers/pci and drivers/e1000 added to Appendix A.
#   58  mm/swap and security/random added to Appendix A.
#   60  block/io_scheduler and block/request_merge added.
#   61  core/driver_init added, once type aliases and indirect calls landed.
#   64  net/socket, net/tcp and net/arp added — the set is import-closed.
64
