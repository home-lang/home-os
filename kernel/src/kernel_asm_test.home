// Test kernel with inline assembly support
// Tests the new asm() feature for kernel development

export fn kernel_main(magic: u32, boot_info: u32) -> never {
  // Disable interrupts
  asm("cli")

  // Halt the CPU
  asm("hlt")

  // Infinite loop (should never reach here)
  loop {
    // Empty loop
  }
}
